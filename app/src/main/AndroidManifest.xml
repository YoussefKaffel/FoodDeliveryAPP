<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="youssef.kaffel.pfa">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PFA">
        <activity android:name=".RecomandedActivity"></activity>
        <activity android:name=".SaladeActivity" />
        <activity android:name=".FinCommandeActivity" />
        <activity android:name=".PetitDejActivity" />
        <activity android:name=".DessertActivity" />
        <activity android:name=".MealchoiceActivity" />
        <activity android:name=".FactureActivity" />
        <activity
            android:name=".CommandeActivity"
            android:label="@string/title_activity_commande"
            android:theme="@style/Theme.PFA.NoActionBar" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>